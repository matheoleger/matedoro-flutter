import 'package:flutter/material.dart';

class HistoryItem extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("history item"),
    );
  }
}